<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/DownGradLogin</item>
        <item name="colorPrimaryDark">#252E06</item>
        <item name="colorAccent">#0f2a01</item>
    </style>

    <style name="SemBarra" parent="Theme.AppCompat.NoActionBar">
        <!-- Sem barra em cima -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="LoginCadastro" parent="Theme.AppCompat.NoActionBar">
        <!-- Sem barra em cima -->
        <item name="colorPrimary">@color/UpGradLogin</item>
        <item name="colorPrimaryDark">@color/UpGradLogin</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="PauseDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowAnimationStyle">@style/PauseDialogAnimation</item>
    </style>

    <style name="PauseDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/shake</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>


    <style name="Splash" parent="Theme.AppCompat.NoActionBar">
        <!-- Sem barra em cima -->
        <item name="colorPrimary">@color/colorSplash</item>
        <item name="colorPrimaryDark">@color/colorSplash</item>
        <item name="colorAccent">@color/colorSplash</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
