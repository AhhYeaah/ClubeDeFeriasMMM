<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clubeseis">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SemBarra">
        <activity
            android:name=".login_and_splashs.Splash"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Cadastro.Check"
            android:theme="@style/LoginCadastro" />
        <activity
            android:name=".app_pos_login.TelaAplicativo"
            android:label="@string/title_activity_tela_aplicativo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".login_and_splashs.LoginActivity"
            android:theme="@style/LoginCadastro" />
    </application>

</manifest>